<!--
  Keep the three headings below exactly as they are.
  The release-notes webhook parses them verbatim; renaming a heading means
  your PR will be ingested with empty content.
-->

## Type

<!-- Delete all but one: -->

New field / New component / Update / Bug fix

## What changed

<!--
  Write this for the end user, not for the reviewer.
  One to three sentences, no ticket numbers, no internal service names.
  This text is what gets polished into the release note that agents read.
-->

## Path(s) in app

<!--
  Where in the product this shows up, one nav path per line, as a bullet.
  Use the same ">" separator the nav uses. Examples:
    - Settings > Profile
    - Reports > Payouts > Adjustments
  Leave the list empty only if this affects every area of the app.
-->

-
